
/*Напишите функцию firstFunction, которая выводит в консоль переменную first.  */

const first = 8
function firstFunction() {
	return first
}
console.log(firstFunction())

/* Напишите функцию sum, которая принимает два параметра a и b,
 складывает их и возвращает результат сложения. Выведите в консоль результат выполнения функции. */

 let a = 5
 let b = 3
 function sum(a, b) {
   return a+b
 }
 console.log(sum(a,b))


/* Напишите функцию multiply, которая принимает два параметра x и y, 
перемножает их и возвращает результат умножения. Выведите в консоль результат выполнения функции. */

let x = 4
let y = 2
function multiply(x, y) {
  return x * y
}
console.log(multiply(x,y))
